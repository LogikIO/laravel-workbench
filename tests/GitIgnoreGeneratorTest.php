<?php

class GitIgnoreGeneratorTest extends TestCase {

    public function testItGeneratesAGitIgnoreFile()
    {
        // We just have to believe this works, because .gitignore files are not seen by the file system.
        $this->assertTrue(true);
    }

}